(function($) {
	
	$.fn.quotate = function(options) {
		
		var defaults = {
			type: "json",
			interval: 10
		};
		var options = $.extend(defaults, options);
		
		return this.each(function() {
			obj = $(this);
			
		});
	};
	
}) (jQuery);